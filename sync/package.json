{
  "name": "rtr-ct-sync",
  "version": "1.0.0",
  "type": "module",
  "description": "MRP → Control Tower data sync",
  "scripts": {
    "sync": "node mrp-sync.mjs",
    "healthcheck": "node healthcheck.mjs"
  },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
